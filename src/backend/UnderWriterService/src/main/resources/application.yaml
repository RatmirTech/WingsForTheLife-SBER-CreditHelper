server:
  port: 8081
spring:
  datasource:
    username: user
    password: password
    url: jdbc:postgresql://db:5432/applications
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect